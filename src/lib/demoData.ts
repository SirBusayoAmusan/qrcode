import type { Channel, TapframePage, Lead } from '../types';

// Default to clean empty state — creators create their own brand & pages from scratch
export const INITIAL_CHANNELS: Channel[] = [];

export const INITIAL_PAGES: TapframePage[] = [];

export const INITIAL_LEADS: Lead[] = [];
